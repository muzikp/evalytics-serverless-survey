{
  "name": "@evalytics/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "sam local start-api --warm-containers EAGER",
    "build": "echo \"API build: (no-op)\" && exit 0",
    "prebuild": "node prebuild.mjs",
    "test": "echo \"TODO: unit tests\" && exit 0",
    "lint": "echo \"TODO: eslint\" && exit 0",
    "sam:validate": "sam validate",
    "sam:build": "sam build",
    "sam:local": "sam local start-api --warm-containers EAGER --skip-pull-image",
    "sam:deploy": "sam deploy --resolve-s3 --capabilities CAPABILITY_IAM"
  },
  "dependencies": {
    "mysql2": "^3.11.5",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "yaml": "^2.3.4"
  }
}