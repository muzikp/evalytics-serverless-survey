openapi: 3.1.0
info:
  title: Evalytics Serverless Survey API
  version: 0.1.0
  description:
    "REST API for managing SurveyJS-based questionnaire templates, snapshots, campaigns, respondents, and responses,

    and for serving public survey endpoints to respondents.


    **Implementation Status: COMPLETED ✅**

    - All core endpoints implemented and tested (2026-01-03)

    - Local testing with SAM local fully functional

    - Known bugs fixed (header case-sensitivity, LIMIT/OFFSET parameters)

    - See API/TEST-REPORT.md for detailed test results


    **Placeholders (AWS Services Required)**

    - Email sending via SQS (POST /campaigns/:id/send) - returns 202 with note

    - S3 file uploads/downloads (POST /attachments/upload, GET /attachments/:id/download) - returns mock URLs

    - Email worker Lambda functions (worker.js, emailEvents.js) - stubs exist


    Audience

    - Admin UI (Svelte static web) for internal users

    - Public survey runtime for respondents


    Auth

    - Admin endpoints: JWT bearer token (1h expiry, recommended) and/or static API token per user (permanent, with scopes)

    - Public endpoints: respondent token provided in invitation link or campaign setup

    - **Note**: Header parsing is case-insensitive (X-Api-Token, X-API-Token, x-api-token all work)


    Notes

    - CORS origin allow-list helps browsers but is not a security control on its own.

    - All timestamps are ISO 8601 in UTC timezone.

    - Pagination: Use ?limit=N&offset=M query parameters (defaults: limit=50, offset=0)

    - All JSON fields in database (title, description, data, etc.) are automatically parsed/stringified

    "
  contact:
    name: Evalytics s.r.o.
    email: info@evalytics.cz
  license:
    name: Proprietary
servers:
  - url: http://127.0.0.1:3000
    description: Local (SAM)
  - url: https://api.evalytics.cz
    description: Production (planned)
  - url: https://{apiId}.execute-api.{region}.amazonaws.com/{stage}
    description: AWS API Gateway
    variables:
      apiId:
        default: xxxxxxxxxx
      region:
        default: eu-central-1
      stage:
        default: prod
        enum:
          - dev
          - test
          - prod
tags:
  - name: Auth
    description: Authentication and session management
  - name: Config
    description: Initial DB schema setup / migrations
  - name: Users
    description: User management (master-admin only)
  - name: Templates
    description: Survey template CRUD
  - name: Snapshots
    description: Template versions (recommended immutable once published)
  - name: Campaigns
    description: Publishing snapshots and sending emails
  - name: EmailAudit
    description: Delivery/bounce/complaint tracking for emails
  - name: Responses
    description: Collected responses and metadata
  - name: Public
    description: Endpoints used by respondents without admin login
  - name: Unsubscribe
    description: Email opt-out / blacklist
  - name: Storage
    description: File metadata + uploads (typically S3 presigned URLs)
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Admin session JWT returned from POST /auth
    apiTokenAuth:
      type: apiKey
      in: header
      name: X-API-Token
      description: Static per-user API token for admin endpoints.
    respondentToken:
      type: apiKey
      in: header
      name: X-Respondent-Token
      description:
        "Respondent access token derived from invitation link (recommended: token delivered in URL fragment and
        then sent in this header)."
    respondentTokenQuery:
      type: apiKey
      in: query
      name: token
      description:
        (Deprecated) Respondent access token in query string for backward compatibility. Prefer X-Respondent-Token
        header.
      deprecated: true
  schemas:
    Error:
      type: object
      properties:
        code:
          type: string
        message:
          type: string
        details:
          type: object
          additionalProperties: true
        request_id:
          type: string
      required:
        - code
        - message
    Pagination:
      type: object
      properties:
        limit:
          type: integer
          minimum: 1
          maximum: 500
          default: 50
        offset:
          type: integer
          minimum: 0
          default: 0
        total:
          type: integer
          minimum: 0
    RoleSet:
      type: object
      additionalProperties: true
      description: 'JSON object for roles/permissions. Minimal: {"admin":1}.'
    User:
      type: object
      properties:
        user_id:
          type: string
          maxLength: 16
        firstname:
          type: string
          maxLength: 256
        lastname:
          type: string
          maxLength: 256
        email:
          type: string
          format: email
          maxLength: 256
        roles:
          $ref: "#/components/schemas/RoleSet"
        created:
          type: string
          format: date-time
        last_update:
          type: string
          format: date-time
      required:
        - user_id
        - firstname
        - lastname
        - email
        - roles
        - created
        - last_update
    UserUpsert:
      type: object
      properties:
        user_id:
          type: string
          maxLength: 16
          description: If omitted, server generates.
        firstname:
          type: string
          maxLength: 256
        lastname:
          type: string
          maxLength: 256
        email:
          type: string
          format: email
          maxLength: 256
        password:
          type: string
          format: password
          description: Plaintext over TLS; stored hashed.
        roles:
          $ref: "#/components/schemas/RoleSet"
        rotate_api_token:
          type: boolean
          default: false
      required:
        - firstname
        - lastname
        - email
        - roles
    TemplateCreate:
      type: object
      properties:
        name:
          type: string
          maxLength: 256
        surveyjs_version:
          type: string
          maxLength: 16
        surveyjs_template:
          type: string
          maxLength: 64
        languages:
          type: array
          items:
            type: string
        data:
          type: object
          additionalProperties: true
          description: SurveyJS JSON definition.
      required:
        - name
        - surveyjs_version
        - languages
        - data
    Template:
      description: Complete template with full SurveyJS data. Note - user names (firstname/lastname) are not included; only user_id references.
      allOf:
        - $ref: "#/components/schemas/TemplateCreate"
        - type: object
          properties:
            template_id:
              type: string
              maxLength: 16
            created:
              type: string
              format: date-time
            last_update:
              type: string
              format: date-time
            created_by:
              type: string
              maxLength: 16
              description: User ID who created the template
            last_modified_by:
              type: string
              maxLength: 16
              description: User ID who last modified the template
          required:
            - template_id
            - created
            - last_update
            - created_by
            - last_modified_by
    TemplateListItem:
      type: object
      description: Template list item without the full SurveyJS data payload
      properties:
        template_id:
          type: string
          maxLength: 16
        name:
          type: string
          maxLength: 256
        surveyjs_version:
          type: string
          maxLength: 16
        languages:
          type: array
          items:
            type: string
        created:
          type: string
          format: date-time
        last_update:
          type: string
          format: date-time
        created_by:
          type: string
          maxLength: 16
          description: User ID who created the template
        last_modified_by:
          type: string
          maxLength: 16
          description: User ID who last modified the template
      required:
        - template_id
        - name
        - surveyjs_version
        - languages
        - created
        - last_update
        - created_by
        - last_modified_by
    SnapshotCreate:
      type: object
      properties:
        template_id:
          type: string
          maxLength: 16
        note:
          type: string
        lock:
          type: boolean
          default: true
      required:
        - template_id
    Snapshot:
      type: object
      description: "Complete snapshot with full SurveyJS data. Note - user names (firstname/lastname) are not included; only user_id references."
      properties:
        snapshot_id:
          type: string
          maxLength: 16
        template_id:
          type: string
          maxLength: 16
        template_name:
          type: string
          description: Name of the source template
        version:
          type: integer
          minimum: 1
          description: Snapshot version number (auto-incremented per template)
        surveyjs_version:
          type: string
          maxLength: 16
        surveyjs_template:
          type: string
          maxLength: 64
        languages:
          type: array
          items:
            type: string
        data:
          type: object
          additionalProperties: true
          description: Full SurveyJS configuration JSON
        note:
          type: string
          description: Optional note about this snapshot version
        locked:
          type: boolean
          default: true
          description: If true, snapshot cannot be modified
        created:
          type: string
          format: date-time
        last_update:
          type: string
          format: date-time
        created_by:
          type: string
          maxLength: 16
          description: User ID who created the snapshot
        last_modified_by:
          type: string
          maxLength: 16
          description: User ID who last modified the snapshot
      required:
        - snapshot_id
        - template_id
        - version
        - surveyjs_version
        - languages
        - data
        - created
        - last_update
        - created_by
        - last_modified_by
    SnapshotListItem:
      type: object
      description: "Snapshot list item without the full SurveyJS data payload (similar to templates). Use GET /snapshots/{id} to retrieve full data."
      properties:
        snapshot_id:
          type: string
          maxLength: 16
        template_id:
          type: string
          maxLength: 16
        template_name:
          type: string
          description: Name of the source template
        version:
          type: integer
          minimum: 1
          description: Snapshot version number
        surveyjs_version:
          type: string
          description: SurveyJS library version
        surveyjs_template:
          type: string
          description: SurveyJS theme/template name
        languages:
          type: array
          items:
            type: string
          description: Supported language codes
        note:
          type: string
          description: Optional note about this snapshot
        locked:
          type: boolean
          description: If true, snapshot cannot be modified
        created:
          type: string
          format: date-time
        last_update:
          type: string
          format: date-time
        created_by:
          type: string
          description: User ID who created the snapshot
        last_modified_by:
          type: string
          description: User ID who last modified the snapshot
      required:
        - snapshot_id
        - template_id
        - version
        - languages
        - last_update
    CampaignCreate:
      type: object
      properties:
        snapshot_id:
          type: string
          maxLength: 16
        public_id:
          type: string
          maxLength: 256
          description: If omitted, server may generate.
        title:
          type: object
          additionalProperties:
            type: string
        description:
          type: object
          additionalProperties:
            type: string
        email_template:
          type: object
          additionalProperties: true
        open_on:
          type:
            - string
            - "null"
          format: date-time
        close_on:
          type:
            - string
            - "null"
          format: date-time
        allow_multiple_responses:
          type: boolean
          default: false
          description: If true, a single respondent can create multiple response attempts in the same campaign.
        max_attempts:
          type:
            - integer
            - "null"
          minimum: 1
          default: 1
          description: Maximum number of attempts per respondent. Null means unlimited (not recommended without safeguards).
        retention:
          $ref: "#/components/schemas/RetentionPolicy"
      required:
        - snapshot_id
        - title
        - description
        - email_template
    Campaign:
      allOf:
        - $ref: "#/components/schemas/CampaignCreate"
        - type: object
          properties:
            campaign_id:
              type: string
              maxLength: 16
            created:
              type: string
              format: date-time
            last_update:
              type: string
              format: date-time
            created_by:
              type: string
              maxLength: 16
            last_modified_by:
              type: string
              maxLength: 16
          required:
            - campaign_id
            - created
            - last_update
            - created_by
            - last_modified_by
    CampaignListItem:
      type: object
      properties:
        campaign_id:
          type: string
        public_id:
          type: string
        snapshot_id:
          type: string
        open_on:
          type:
            - string
            - "null"
          format: date-time
        close_on:
          type:
            - string
            - "null"
          format: date-time
        stats:
          type: object
          properties:
            respondents_total:
              type: integer
              minimum: 0
            responses_total:
              type: integer
              minimum: 0
            responses_completed:
              type: integer
              minimum: 0
            responses_partial:
              type: integer
              minimum: 0
          additionalProperties: false
        last_update:
          type: string
          format: date-time
      required:
        - campaign_id
        - public_id
        - snapshot_id
        - stats
        - last_update
    Respondent:
      type: object
      properties:
        respondent_id:
          type: string
          maxLength: 32
        campaign_id:
          type: string
          maxLength: 16
        email:
          type: string
          format: email
          maxLength: 256
        token:
          type: string
          maxLength: 128
          description: Recommended to store hashed token server-side.
        data:
          type: object
          additionalProperties: true
        created:
          type: string
          format: date-time
        last_update:
          type: string
          format: date-time
      required:
        - respondent_id
        - campaign_id
        - email
        - created
        - last_update
    Response:
      type: object
      properties:
        response_id:
          type: string
          maxLength: 64
        respondent_id:
          type: string
          maxLength: 32
        request_data:
          type: object
          additionalProperties: true
        data:
          type: object
          additionalProperties: true
        is_complete:
          type: boolean
          deprecated: true
          description: "Deprecated: use status='completed'."
        created:
          type: string
          format: date-time
        last_update:
          type: string
          format: date-time
        campaign_id:
          type: string
          maxLength: 16
          description: Convenience denormalization.
        snapshot_id:
          type: string
          maxLength: 16
          description: Convenience denormalization.
        attempt_no:
          type: integer
          minimum: 1
          description: Attempt number for a given respondent within a campaign.
        status:
          $ref: "#/components/schemas/ResponseStatus"
        client_meta:
          type: object
          additionalProperties: true
          description: Optional client telemetry (time spent, device hints, etc.).
        submitted_at:
          type:
            - string
            - "null"
          format: date-time
        attachments:
          type: array
          items:
            $ref: "#/components/schemas/ResponseAttachment"
      required:
        - data
        - status
        - last_update
        - created
        - response_id
        - respondent_id
        - attempt_no
    ResponseUpsert:
      type: object
      properties:
        data:
          type: object
          additionalProperties: true
        is_complete:
          type: boolean
          deprecated: true
          description: "Deprecated: use status='completed'."
        client_meta:
          type: object
          additionalProperties: true
        response_id:
          type: string
          maxLength: 64
          description:
            If provided, updates this specific attempt. If omitted, server updates latest open attempt (or creates
            attempt 1).
        status:
          $ref: "#/components/schemas/ResponseStatus"
      required:
        - data
    AuthRequest:
      type: object
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
      required:
        - email
        - password
    AuthResponse:
      type: object
      properties:
        token:
          type: string
        expires_in:
          type: integer
        user:
          $ref: "#/components/schemas/User"
      required:
        - token
        - expires_in
        - user
    ApiToken:
      type: object
      description:
        "Long-lived personal access token (PAT) for programmatic REST access. The plaintext token value is only
        shown once on creation; only hashes are stored.

        "
      properties:
        token_id:
          type: string
          maxLength: 32
        user_id:
          type: string
          maxLength: 16
        name:
          type: string
          maxLength: 128
        scopes:
          type: array
          items:
            type: string
        created:
          type: string
          format: date-time
        last_used:
          type: string
          format: date-time
          nullable: true
        expires_at:
          type: string
          format: date-time
          nullable: true
        revoked_at:
          type: string
          format: date-time
          nullable: true
      required:
        - token_id
        - user_id
        - name
        - scopes
        - created
    ApiTokenCreateRequest:
      type: object
      properties:
        name:
          type: string
          maxLength: 128
        scopes:
          type: array
          items:
            type: string
          default: []
        expires_at:
          type: string
          format: date-time
          nullable: true
      required:
        - name
    ApiTokenCreateResponse:
      type: object
      properties:
        token:
          type: string
          description: Plaintext token value (returned only once).
        api_token:
          $ref: "#/components/schemas/ApiToken"
      required:
        - token
        - api_token
    ApiTokenList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ApiToken"
      required:
        - items
    ConfigRequest:
      type: object
      properties:
        reset:
          type: boolean
          default: false
        schema:
          type: string
      required:
        - schema
    ConfigResponse:
      type: object
      properties:
        ok:
          type: boolean
        log:
          type: array
          items:
            type: string
      required:
        - ok
        - log
    EmailSendRequest:
      type: object
      properties:
        mode:
          type: string
          enum:
            - initial
            - reminder
          default: initial
        dry_run:
          type: boolean
          default: false
        only_unsent:
          type: boolean
          default: true
        limit:
          type: integer
          minimum: 1
          maximum: 5000
        locale_fallback:
          type: string
          default: en-GB
        reminder_targets:
          type: string
          enum:
            - not_started
            - incomplete
            - both
          default: both
          description: "For reminder mode: choose which respondents to target."
        incomplete_grace_minutes:
          type: integer
          minimum: 0
          default: 60
          description: "For reminder mode + incomplete targets: do not remind if respondent was active within this many minutes."
      additionalProperties: false
    EmailSendStatus:
      type: object
      properties:
        campaign_id:
          type: string
        mode:
          type: string
          enum:
            - initial
            - reminder
        counts:
          type: object
          properties:
            queued:
              type: integer
              minimum: 0
            sent:
              type: integer
              minimum: 0
            bounced:
              type: integer
              minimum: 0
            complained:
              type: integer
              minimum: 0
            failed:
              type: integer
              minimum: 0
          additionalProperties: false
        last_update:
          type: string
          format: date-time
      required:
        - campaign_id
        - mode
        - counts
        - last_update
    EmailLogEntry:
      type: object
      description: Per-recipient email send record with delivery audit fields.
      properties:
        email_log_id:
          type: integer
          format: int64
        campaign_id:
          type: string
          maxLength: 16
        respondent_id:
          type: string
          maxLength: 32
        recipient_email:
          type: string
          format: email
        email_type:
          type: string
          enum: [invite, reminder]
        provider:
          type: string
          enum: [ses]
        provider_message_id:
          type:
            - string
            - "null"
        status:
          type: string
          enum: [queued, sent, delivered, bounced, complaint, rejected, failed]
        last_event_type:
          type:
            - string
            - "null"
        last_event_at:
          type:
            - string
            - "null"
          format: date-time
        error_message:
          type:
            - string
            - "null"
        created:
          type: string
          format: date-time
        last_update:
          type: string
          format: date-time
      required:
        - email_log_id
        - campaign_id
        - respondent_id
        - recipient_email
        - email_type
        - provider
        - status
        - created
        - last_update
      additionalProperties: false
    EmailLogList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/EmailLogEntry"
        page:
          $ref: "#/components/schemas/Pagination"
      required: [items, page]
      additionalProperties: false
    EmailDeliveryEvent:
      type: object
      description: Provider delivery event (raw-ish) stored for audit.
      properties:
        event_id:
          type: integer
          format: int64
        provider:
          type: string
          enum: [ses]
        provider_message_id:
          type: string
        event_type:
          type: string
        event_at:
          type: string
          format: date-time
        recipient_email:
          type:
            - string
            - "null"
          format: email
        payload_json:
          type:
            - object
            - "null"
          additionalProperties: true
      required:
        - event_id
        - provider
        - provider_message_id
        - event_type
        - event_at
      additionalProperties: false
    EmailDeliveryEventList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/EmailDeliveryEvent"
        page:
          $ref: "#/components/schemas/Pagination"
      required: [items, page]
      additionalProperties: false

    SurveyPublic:
      type: object
      properties:
        public_id:
          type: string
        snapshot:
          $ref: "#/components/schemas/Snapshot"
        campaign:
          $ref: "#/components/schemas/Campaign"
        response:
          oneOf:
            - $ref: "#/components/schemas/ResponsePublic"
            - type: "null"
        locale:
          type: string
        responses:
          type: array
          items:
            $ref: "#/components/schemas/ResponsePublic"
          description: "Optional: all attempts (if requested)."
      required:
        - public_id
        - snapshot
        - campaign
    UnsubscribeTokenRequest:
      type: object
      description: Unsubscribe action invoked from email footer. Uses a signed token so the API never needs to accept raw email in query params.
      properties:
        u:
          type: string
          description: Signed unsubscribe token from email footer.
        reason:
          type:
            - string
            - "null"
          maxLength: 256
      required: [u]
      additionalProperties: false
    UnsubscribeResponse:
      type: object
      properties:
        ok:
          type: boolean
        scope:
          type: string
          enum: [snapshot, global]
          description: snapshot = stop emails only for this research/snapshot; global = stop all emails from the platform.
        snapshot_id:
          type:
            - string
            - "null"
          maxLength: 16
        message:
          type: string
      required:
        - ok
        - scope
        - message
      additionalProperties: false
    RespondentUpsert:
      type: object
      properties:
        respondent_id:
          type: string
          maxLength: 32
          description: If omitted, server generates.
        email:
          type: string
          format: email
          maxLength: 256
        data:
          type: object
          additionalProperties: true
        regenerate_token:
          type: boolean
          default: false
      required:
        - email
    BulkResult:
      type: object
      properties:
        ok:
          type: boolean
        created:
          type: integer
          minimum: 0
        updated:
          type: integer
          minimum: 0
        deleted:
          type: integer
          minimum: 0
        errors:
          type: array
          items:
            $ref: "#/components/schemas/Error"
      required:
        - ok
    BlacklistEntry:
      type: object
      properties:
        black_list_id:
          type: integer
        email:
          type: string
          format: email
          maxLength: 256
        scope:
          type: string
          enum: [snapshot, global]
        snapshot_id:
          type:
            - string
            - "null"
          maxLength: 16
          description: Required when scope=snapshot. Must be null when scope=global.
        reason_code:
          type: integer
          description: Enumerated reason code.
        note:
          type:
            - string
            - "null"
        created:
          type: string
          format: date-time
      required:
        - black_list_id
        - email
        - scope
        - created
      additionalProperties: false
    BlacklistUpsert:
      type: object
      properties:
        email:
          type: string
          format: email
          maxLength: 256
        scope:
          type: string
          enum: [snapshot, global]
          default: snapshot
        snapshot_id:
          type:
            - string
            - "null"
          maxLength: 16
        reason_code:
          type: integer
        note:
          type: string
          maxLength: 512
      required:
        - email
        - scope
      additionalProperties: false
    StorageItem:
      type: object
      properties:
        storage_item_id:
          type: string
          maxLength: 32
        location:
          type: string
          description: S3 URI or other remote location.
        local_path:
          type: string
          description: Logical folder path used in UI.
        name:
          type: string
          maxLength: 256
        description:
          type: string
        filetype:
          type: string
          maxLength: 256
        size_bytes:
          type: integer
          minimum: 0
        created:
          type: string
          format: date-time
        last_update:
          type: string
          format: date-time
        created_by:
          type: string
          maxLength: 16
        last_modified_by:
          type: string
          maxLength: 16
      required:
        - storage_item_id
        - name
        - created
        - last_update
    StorageCreateRequest:
      type: object
      properties:
        name:
          type: string
          maxLength: 256
        filetype:
          type: string
          maxLength: 256
          description: MIME type (recommended)
        local_path:
          type: string
        description:
          type: string
        size_bytes:
          type: integer
          minimum: 0
      required:
        - name
        - filetype
    PresignedUpload:
      type: object
      properties:
        storage_item:
          $ref: "#/components/schemas/StorageItem"
        upload:
          type: object
          properties:
            method:
              type: string
              enum:
                - PUT
                - POST
            url:
              type: string
            headers:
              type: object
              additionalProperties:
                type: string
            fields:
              type: object
              additionalProperties:
                type: string
            expires_in:
              type: integer
          required:
            - method
            - url
            - expires_in
      required:
        - storage_item
        - upload
    ResponseStatus:
      type: string
      description: Logical status of a response attempt.
      enum:
        - in_progress
        - completed
    RetentionPolicy:
      type: object
      description:
        "Retention policy for personal data. NOTE: GDPR does not define a fixed maximum retention period; controller
        must define limits based on necessity/proportionality and/or legal obligations."
      properties:
        identifiable_retention_days:
          type:
            - integer
            - "null"
          minimum: 1
          description:
            How long responses may be stored in an identifiable form (email/token-hash linkable to a person). Null
            disables auto-expiry (not recommended unless data are anonymised).
        after_retention_action:
          type: string
          enum:
            - delete
            - anonymize
          default: anonymize
          description:
            "What to do after identifiable_retention_days: delete all data or anonymize (remove identifiers, keep
            aggregated/anonymous answers)."
        review_every_days:
          type: integer
          minimum: 1
          default: 365
          description: Periodic review cadence for retention necessity.
        legal_hold:
          type: boolean
          default: false
          description: If true, automated deletion/anonymisation is paused for this campaign.
      additionalProperties: false
    ResponseAttachment:
      type: object
      properties:
        upload_id:
          type: string
          maxLength: 64
          description: Upload session ID.
        storage_item_id:
          type: string
          maxLength: 16
        question_name:
          type: string
          maxLength: 256
          description: SurveyJS question name / field that owns this file.
        original_name:
          type: string
          maxLength: 512
        content_type:
          type: string
          maxLength: 128
          description: "Allowed categories: images (image/*), documents (pdf/doc/docx/xls/xlsx/ppt/pptx/txt), audio (audio/*), video (video/*)."
        size_bytes:
          type: integer
          minimum: 0
        created:
          type: string
          format: date-time
      required:
        - upload_id
        - storage_item_id
        - question_name
        - original_name
        - created
      additionalProperties: false
    ResponsePublic:
      type: object
      properties:
        response_id:
          type: string
          maxLength: 64
        attempt_no:
          type: integer
          minimum: 1
        status:
          $ref: "#/components/schemas/ResponseStatus"
        data:
          type: object
          additionalProperties: true
        submitted_at:
          type:
            - string
            - "null"
          format: date-time
        created:
          type: string
          format: date-time
        last_update:
          type: string
          format: date-time
        attachments:
          type: array
          items:
            $ref: "#/components/schemas/ResponseAttachment"
      required:
        - response_id
        - attempt_no
        - status
        - data
        - created
        - last_update
      additionalProperties: false
    PublicUploadInitRequest:
      type: object
      properties:
        response_id:
          type: string
          maxLength: 64
          description: Target response attempt. If omitted, server uses latest open attempt (or creates attempt 1).
        question_name:
          type: string
          maxLength: 256
        original_name:
          type: string
          maxLength: 512
        content_type:
          type: string
          maxLength: 128
        size_bytes:
          type: integer
          minimum: 1
          maximum: 52428800
          description: Maximum 50 MB per file.
        checksum_sha256:
          type:
            - string
            - "null"
          maxLength: 128
          description: Optional checksum to validate upload.
      required:
        - question_name
        - original_name
        - content_type
        - size_bytes
      additionalProperties: false
    PublicUploadInitResponse:
      type: object
      properties:
        upload_id:
          type: string
          maxLength: 64
        response_id:
          type: string
          maxLength: 64
        storage_item_id:
          type: string
          maxLength: 16
        upload:
          $ref: "#/components/schemas/PresignedUpload"
        expires_at:
          type: string
          format: date-time
        max_size_bytes:
          type: integer
          minimum: 1
      required:
        - upload_id
        - response_id
        - storage_item_id
        - upload
        - expires_at
        - max_size_bytes
      additionalProperties: false
    PublicUploadCompleteRequest:
      type: object
      properties:
        etag:
          type:
            - string
            - "null"
          description: Optional ETag returned by S3.
      additionalProperties: false
    PublicUploadCompleteResponse:
      type: object
      properties:
        ok:
          type: boolean
        attachment:
          $ref: "#/components/schemas/ResponseAttachment"
      required:
        - ok
        - attachment
      additionalProperties: false
  parameters:
    Limit:
      name: limit
      in: query
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 500
        default: 50
      description: Page size
    Offset:
      name: offset
      in: query
      required: false
      schema:
        type: integer
        minimum: 0
        default: 0
      description: Page offset
    TemplateIdQuery:
      name: template_id
      in: query
      required: false
      schema:
        type: string
        maxLength: 16
      description: Filter by template_id
    CampaignIdQuery:
      name: campaign_id
      in: query
      required: false
      schema:
        type: string
        maxLength: 16
      description: Filter by campaign_id
    SnapshotIdQuery:
      name: snapshot_id
      in: query
      required: false
      schema:
        type: string
        maxLength: 16
      description: Filter by snapshot_id
    RespondentIdQuery:
      name: respondent_id
      in: query
      required: false
      schema:
        type: string
        maxLength: 32
      description: Filter by respondent_id
    IdPath16:
      name: id
      in: path
      required: true
      schema:
        type: string
        maxLength: 16
      description: Entity identifier
    PublicIdPath:
      name: public_id
      in: path
      required: true
      schema:
        type: string
        maxLength: 256
      description: Public campaign id
    TokenQuery:
      name: token
      in: query
      required: true
      schema:
        type: string
        maxLength: 128
      description: Respondent token
  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Forbidden:
      description: Forbidden
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    NotFound:
      description: Not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Conflict:
      description: Conflict
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    ServerError:
      description: Server error
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
security:
  - bearerAuth: []
  - apiTokenAuth: []
paths:
  /auth:
    post:
      tags:
        - Auth
      summary: Authenticate admin user
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthRequest"
      responses:
        "200":
          description: Authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/ServerError"
  /auth/me:
    get:
      tags:
        - Auth
      summary: Get current admin session
      description: Returns the user associated with the provided bearer JWT or X-API-Token.
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/ServerError"
  /api-tokens:
    get:
      tags:
        - Auth
      summary: List personal access tokens
      description: Lists PAT metadata for the authenticated user. Does not return token value.
      responses:
        "200":
          description: Token list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiTokenList"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Auth
      summary: Create personal access token
      description: Creates a new PAT for programmatic REST access. The plaintext token is returned only once.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApiTokenCreateRequest"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiTokenCreateResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/ServerError"
  /api-tokens/{token_id}:
    delete:
      tags:
        - Auth
      summary: Revoke personal access token
      parameters:
        - name: token_id
          in: path
          required: true
          schema:
            type: string
            maxLength: 32
      responses:
        "204":
          description: Revoked
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
  /config:
    post:
      tags:
        - Config
      summary: Initialize or migrate database schema
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConfigRequest"
      responses:
        "200":
          description: Migration result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConfigResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /users:
    get:
      tags:
        - Users
      summary: Get user
      description: Master-admin only. Fetch a user by user_id or email.
      parameters:
        - name: user_id
          in: query
          required: false
          schema:
            type: string
            maxLength: 16
        - name: email
          in: query
          required: false
          schema:
            type: string
            format: email
      responses:
        "200":
          description: User
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Users
      summary: Create or update user
      description: Master-admin only. Upserts a user.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserUpsert"
      responses:
        "200":
          description: Upserted user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
        - Users
      summary: Delete user
      parameters:
        - name: user_id
          in: query
          required: true
          schema:
            type: string
            maxLength: 16
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
  /templates:
    get:
      tags:
        - Templates
      summary: List templates
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Offset"
        - name: id
          in: query
          required: false
          schema:
            type: string
            maxLength: 16
          description: Filter by exact template_id
        - name: q
          in: query
          required: false
          schema:
            type: string
          description: Search by name (substring)
        - name: surveyjs_version
          in: query
          required: false
          schema:
            type: string
          description: Filter by exact SurveyJS version
        - name: languages
          in: query
          required: false
          schema:
            type: string
          description: Filter templates containing this language code (e.g., "en", "cs")
        - name: created_by
          in: query
          required: false
          schema:
            type: string
            maxLength: 16
          description: Filter by user_id who created the template
        - name: last_modified_by
          in: query
          required: false
          schema:
            type: string
            maxLength: 16
          description: Filter by user_id who last modified the template
        - name: created_from
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Filter templates created on or after this timestamp
        - name: created_to
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Filter templates created on or before this timestamp
        - name: updated_from
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Filter templates last updated on or after this timestamp
        - name: updated_to
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Filter templates last updated on or before this timestamp
      responses:
        "200":
          description: Template list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/TemplateListItem"
                  page:
                    $ref: "#/components/schemas/Pagination"
                required:
                  - items
                  - page
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Templates
      summary: Create template
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TemplateCreate"
      responses:
        "200":
          description: Created template
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Template"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /templates/{id}:
    get:
      tags:
        - Templates
      summary: Get template
      parameters:
        - $ref: "#/components/parameters/IdPath16"
      responses:
        "200":
          description: Template
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Template"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Templates
      summary: Update template
      description: Kept as POST to match initial spec; consider PUT/PATCH in future.
      parameters:
        - $ref: "#/components/parameters/IdPath16"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TemplateCreate"
      responses:
        "200":
          description: Updated template
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Template"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
        - Templates
      summary: Delete template
      parameters:
        - $ref: "#/components/parameters/IdPath16"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /snapshots:
    get:
      tags:
        - Snapshots
      summary: List snapshots
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Offset"
        - name: id
          in: query
          required: false
          schema:
            type: string
            maxLength: 16
          description: Filter by exact snapshot_id
        - name: template_id
          in: query
          required: false
          schema:
            type: string
            maxLength: 16
          description: Filter by template_id
        - name: version
          in: query
          required: false
          schema:
            type: integer
          description: Filter by exact version number
        - name: locked
          in: query
          required: false
          schema:
            type: boolean
          description: Filter by locked status
        - name: surveyjs_version
          in: query
          required: false
          schema:
            type: string
          description: Filter by exact SurveyJS version
        - name: languages
          in: query
          required: false
          schema:
            type: string
          description: Filter snapshots containing this language code (e.g., "en", "cs")
        - name: created_by
          in: query
          required: false
          schema:
            type: string
            maxLength: 16
          description: Filter by user_id who created the snapshot
        - name: last_modified_by
          in: query
          required: false
          schema:
            type: string
            maxLength: 16
          description: Filter by user_id who last modified the snapshot
        - name: created_from
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Filter snapshots created on or after this timestamp
        - name: created_to
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Filter snapshots created on or before this timestamp
        - name: updated_from
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Filter snapshots last updated on or after this timestamp
        - name: updated_to
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Filter snapshots last updated on or before this timestamp
      responses:
        "200":
          description: Snapshot list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/SnapshotListItem"
                  page:
                    $ref: "#/components/schemas/Pagination"
                required:
                  - items
                  - page
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Snapshots
      summary: Create snapshot from template
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SnapshotCreate"
      responses:
        "200":
          description: Created snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Snapshot"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /snapshots/{id}:
    get:
      tags:
        - Snapshots
      summary: Get snapshot
      parameters:
        - $ref: "#/components/parameters/IdPath16"
      responses:
        "200":
          description: Snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Snapshot"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Snapshots
      summary: Update snapshot (discouraged)
      description: Recommended to keep snapshots immutable once used in campaigns.
      parameters:
        - $ref: "#/components/parameters/IdPath16"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                surveyjs_version:
                  type: string
                surveyjs_template:
                  type: string
                languages:
                  type: array
                  items:
                    type: string
                data:
                  type: object
                  additionalProperties: true
                lock:
                  type: boolean
              required:
                - data
      responses:
        "200":
          description: Updated snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Snapshot"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
        - Snapshots
      summary: Delete snapshot
      parameters:
        - $ref: "#/components/parameters/IdPath16"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /campaigns:
    get:
      tags:
        - Campaigns
      summary: List campaigns
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Offset"
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum:
              - scheduled
              - open
              - closed
              - all
            default: all
          description: Computed from open_on/close_on.
      responses:
        "200":
          description: Campaign list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/CampaignListItem"
                  page:
                    $ref: "#/components/schemas/Pagination"
                required:
                  - items
                  - page
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Campaigns
      summary: Create campaign
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CampaignCreate"
      responses:
        "200":
          description: Created campaign
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Campaign"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /campaigns/{id}:
    get:
      tags:
        - Campaigns
      summary: Get campaign details
      description: Returns campaign including respondent list and response statuses (implementation-defined).
      parameters:
        - $ref: "#/components/parameters/IdPath16"
      responses:
        "200":
          description: Campaign details
          content:
            application/json:
              schema:
                type: object
                properties:
                  campaign:
                    $ref: "#/components/schemas/Campaign"
                  respondents:
                    type: array
                    items:
                      $ref: "#/components/schemas/Respondent"
                  responses:
                    type: array
                    items:
                      $ref: "#/components/schemas/Response"
                required:
                  - campaign
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Campaigns
      summary: Update campaign
      parameters:
        - $ref: "#/components/parameters/IdPath16"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CampaignCreate"
      responses:
        "200":
          description: Updated campaign
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Campaign"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
        - Campaigns
      summary: Delete campaign
      parameters:
        - $ref: "#/components/parameters/IdPath16"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /campaigns/{id}/send:
    get:
      tags:
        - Campaigns
      summary: Get email sending status
      parameters:
        - $ref: "#/components/parameters/IdPath16"
      responses:
        "200":
          description: Status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailSendStatus"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Campaigns
      summary: Send emails
      parameters:
        - $ref: "#/components/parameters/IdPath16"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmailSendRequest"
      responses:
        "200":
          description: Send result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailSendStatus"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /responses:
    get:
      tags:
        - Responses
      summary: List responses with universal filtering
      description: |
        Universal endpoint for listing responses with flexible filtering options.
        Supports filtering by campaign, form, status, date ranges, and text enhancement.
        
        **Authorization:**
        - Admins: Can view all responses
        - Respondents: Can only view their own responses (filtered by email automatically)
        
        **Authentication:**
        - Admin: Use `Authorization: Bearer <jwt>` or `X-API-Token: <token>` header
        - Respondent: Use `X-Respondent-Token: <token>` header
        
        **Filter by campaign or form:**
        - `campaign_id` - Filter by specific campaign
        - `form_id` - Filter by form (can include responses from multiple campaigns)
        
        **Filter by status:**
        - `response_status` - Can be specified multiple times (e.g., ?response_status=in_progress&response_status=completed)
        - Allowed values: "in_progress", "completed"
        
        **Filter by dates:**
        - `finished_before` / `finished_after` - Filter by submitted_at (completion date)
        - `created_before` / `created_after` - Filter by created (response creation)
        - Date format: ISO 8601 (e.g., "2026-01-10T12:00:00Z")
        
        **Text enhancement:**
        - `includeQuestionText=true` - Add human-readable question text from form definition
        - `includeAnswerText=true` - Add human-readable answer text for choice-based questions
        
        **Format:**
        - `format=json` (default) - JSON response
        - `format=xlsx` - Excel export (not yet implemented, returns 501)
        
        **Pagination:**
        - `limit` (default: 50) - Number of results per page
        - `offset` (default: 0) - Skip N results
      security:
        - bearerAuth: []
        - apiToken: []
        - respondentToken: []
      parameters:
        - name: campaign_id
          in: query
          required: false
          schema:
            type: string
            maxLength: 16
          description: Filter by campaign ID
        - name: form_id
          in: query
          required: false
          schema:
            type: string
            maxLength: 16
          description: Filter by form ID (responses from all campaigns using this form)
        - name: response_status
          in: query
          required: false
          schema:
            type: array
            items:
              type: string
              enum: [in_progress, completed]
          style: form
          explode: true
          description: Filter by response status (can specify multiple)
        - name: finished_before
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Filter responses finished before this date (submitted_at < value)
        - name: finished_after
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Filter responses finished after this date (submitted_at >= value)
        - name: created_before
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Filter responses created before this date
        - name: created_after
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Filter responses created after this date
        - name: includeQuestionText
          in: query
          required: false
          schema:
            type: boolean
            default: false
          description: Include human-readable question text in responses
        - name: includeAnswerText
          in: query
          required: false
          schema:
            type: boolean
            default: false
          description: Include human-readable answer text for choice-based questions
        - name: format
          in: query
          required: false
          schema:
            type: string
            enum: [json, xlsx]
            default: json
          description: Export format (xlsx not yet implemented)
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Offset"
      responses:
        "200":
          description: Response list with applied filters
          content:
            application/json:
              schema:
                type: object
                properties:
                  export_date:
                    type: string
                    format: date-time
                    description: Export timestamp
                  filters:
                    type: object
                    description: Applied filters
                    properties:
                      campaign_id:
                        type: string
                      form_id:
                        type: string
                      response_status:
                        type: array
                        items:
                          type: string
                      finished_before:
                        type: string
                      finished_after:
                        type: string
                      created_before:
                        type: string
                      created_after:
                        type: string
                      includeQuestionText:
                        type: boolean
                      includeAnswerText:
                        type: boolean
                  items:
                    type: array
                    items:
                      type: object
                      properties:
                        response_id:
                          type: string
                        respondent_id:
                          type: string
                        email:
                          type: string
                        campaign_id:
                          type: string
                        campaign_public_id:
                          type: string
                        form_id:
                          type: string
                        form_name:
                          type: string
                        form_version:
                          type: integer
                        version_id:
                          type: string
                        attempt_no:
                          type: integer
                        status:
                          type: string
                          enum: [in_progress, completed]
                        submitted_at:
                          type: string
                          format: date-time
                        created:
                          type: string
                          format: date-time
                        last_update:
                          type: string
                          format: date-time
                        data:
                          type: object
                          description: Response data (enhanced with questionText/answerText if requested)
                        request_data:
                          type: object
                        client_meta:
                          type: object
                  page:
                    $ref: "#/components/schemas/Pagination"
                  count:
                    type: integer
                    description: Number of items in current page
                required:
                  - export_date
                  - filters
                  - items
                  - page
                  - count
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "501":
          description: Excel format not yet implemented
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  code:
                    type: string
                  message:
                    type: string
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Responses
      summary: Create new response
      description: |
        Create a new response for a respondent in a campaign.
        Returns the newly created response with generated response_id.
        
        **Required fields:**
        - respondent_id - Must exist in campaign_respondents
        - campaign_id - Campaign must exist and not be removed
        - data - Response data (SurveyJS format)
        
        **Optional fields:**
        - status - "in_progress" or "completed" (default: "in_progress")
        - request_data - HTTP request metadata
        - client_meta - Client-side metadata
        
        **Notes:**
        - Automatically determines attempt_no based on existing responses
        - Checks allow_multiple_responses setting
        - Sets submitted_at automatically if status is "completed"
        - **Admin only** - Respondents cannot use this endpoint
      security:
        - bearerAuth: []
        - apiToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                respondent_id:
                  type: string
                  maxLength: 32
                  description: Respondent ID (must exist in campaign)
                campaign_id:
                  type: string
                  maxLength: 16
                  description: Campaign ID
                data:
                  type: object
                  additionalProperties: true
                  description: Response data (SurveyJS format)
                status:
                  type: string
                  enum: [in_progress, completed]
                  default: in_progress
                  description: Response status
                request_data:
                  type: object
                  description: HTTP request metadata (optional)
                client_meta:
                  type: object
                  description: Client-side metadata (optional)
              required:
                - respondent_id
                - campaign_id
                - data
      responses:
        "200":
          description: Response created successfully
          content:
            application/json:
              schema:
                type: object
                description: Full response object (same as GET /responses/{id})
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Campaign or respondent not found
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  code:
                    type: string
                  message:
                    type: string
        "409":
          description: Multiple responses not allowed
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  code:
                    type: string
                  message:
                    type: string
        "500":
          $ref: "#/components/responses/ServerError"
  /responses/bulk:
    post:
      tags:
        - Responses
      summary: Bulk create responses
      description: |
        Create multiple responses in a single request.
        Useful for importing or batch processing responses.
        
        **Request format:**
        - Body must contain `responses` array
        - Each response must have: respondent_id, campaign_id, data
        - Partial success: Some responses may succeed while others fail
        
        **Response format:**
        - `created_count` - Number of successfully created responses
        - `failed_count` - Number of failed responses
        - `created` - Array of created response summaries
        - `failed` - Array of failed responses with error messages
        
        **Authorization:**
        - **Admin only** - Respondents cannot use this endpoint
      security:
        - bearerAuth: []
        - apiToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                responses:
                  type: array
                  items:
                    type: object
                    properties:
                      respondent_id:
                        type: string
                        maxLength: 32
                      campaign_id:
                        type: string
                        maxLength: 16
                      data:
                        type: object
                        additionalProperties: true
                      status:
                        type: string
                        enum: [in_progress, completed]
                        default: in_progress
                      request_data:
                        type: object
                      client_meta:
                        type: object
                    required:
                      - respondent_id
                      - campaign_id
                      - data
              required:
                - responses
      responses:
        "200":
          description: Bulk operation completed (partial success possible)
          content:
            application/json:
              schema:
                type: object
                properties:
                  created_count:
                    type: integer
                    description: Number of successfully created responses
                  failed_count:
                    type: integer
                    description: Number of failed responses
                  created:
                    type: array
                    items:
                      type: object
                      properties:
                        response_id:
                          type: string
                        respondent_id:
                          type: string
                        campaign_id:
                          type: string
                        status:
                          type: string
                  failed:
                    type: array
                    items:
                      type: object
                      properties:
                        data:
                          type: object
                          description: Original request data
                        error:
                          type: string
                          description: Error message
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /responses/{id}:
    get:
      tags:
        - Responses
      summary: Get single response by ID
      description: |
        Retrieve a single response with full context including form data.
        
        **Format options:**
        - `format=json` (default) - Returns JSON response with full details
        - `format=html` - HTML format (not yet implemented, returns 501)
        - `format=xlsx` - Excel format (not yet implemented, returns 501)
        
        **Response includes:**
        - Full response data and metadata
        - Respondent email
        - Campaign information (ID, public_id)
        - Form information (ID, name, version)
        - Complete form definition (form_data) for processing questions/answers
        
        **Authorization:**
        - Admins: Can view any response
        - Respondents: Can only view their own responses (matched by email)
      security:
        - bearerAuth: []
        - apiToken: []
        - respondentToken: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            maxLength: 64
          description: Response ID
        - name: format
          in: query
          required: false
          schema:
            type: string
            enum: [json, html, xlsx]
            default: json
          description: Response format
      responses:
        "200":
          description: Response details
          content:
            application/json:
              schema:
                type: object
                properties:
                  response_id:
                    type: string
                    maxLength: 64
                  respondent_id:
                    type: string
                    maxLength: 32
                  email:
                    type: string
                    format: email
                  campaign_id:
                    type: string
                    maxLength: 16
                  campaign_public_id:
                    type: string
                  form_id:
                    type: string
                    maxLength: 16
                  form_name:
                    type: string
                  form_version:
                    type: integer
                  version_id:
                    type: string
                    maxLength: 16
                  attempt_no:
                    type: integer
                  status:
                    type: string
                    enum: [in_progress, completed]
                  submitted_at:
                    type: string
                    format: date-time
                    nullable: true
                  created:
                    type: string
                    format: date-time
                  last_update:
                    type: string
                    format: date-time
                  data:
                    type: object
                    description: Response data (SurveyJS answers)
                  request_data:
                    type: object
                    nullable: true
                    description: HTTP request metadata
                  client_meta:
                    type: object
                    nullable: true
                    description: Client-side metadata
                  form_data:
                    type: object
                    nullable: true
                    description: SurveyJS form definition
                required:
                  - response_id
                  - respondent_id
                  - campaign_id
                  - form_id
                  - status
                  - data
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Response not found
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  code:
                    type: string
                  message:
                    type: string
        "501":
          description: Format not yet implemented
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  code:
                    type: string
                  message:
                    type: string
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
        - Responses
      summary: Update existing response
      description: |
        Update an existing response by ID.
        Only provided fields will be updated.
        
        **Updatable fields:**
        - data - Response data (completely replaces existing data)
        - status - Response status (in_progress → completed sets submitted_at)
        - request_data - HTTP request metadata
        - client_meta - Client-side metadata
        
        **Notes:**
        - Returns 404 if response doesn't exist or is removed
        - Automatically updates last_update timestamp
        - Setting status to "completed" sets submitted_at if not already set
        
        **Authorization:**
        - Admins: Can update any response
        - Respondents: Can only update their own responses (matched by email)
      security:
        - bearerAuth: []
        - apiToken: []
        - respondentToken: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            maxLength: 64
          description: Response ID
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                data:
                  type: object
                  additionalProperties: true
                  description: Response data (SurveyJS format)
                status:
                  type: string
                  enum: [in_progress, completed]
                  description: Response status
                request_data:
                  type: object
                  description: HTTP request metadata
                client_meta:
                  type: object
                  description: Client-side metadata
      responses:
        "200":
          description: Response updated successfully
          content:
            application/json:
              schema:
                type: object
                description: Full response object (same as GET /responses/{id})
        "400":
          description: Bad request (no fields to update)
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  code:
                    type: string
                  message:
                    type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Response not found
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  code:
                    type: string
                  message:
                    type: string
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
        - Responses
      summary: Delete response (soft delete)
      description: |
        Soft delete a response by setting removed flag to 1.
        The response data is preserved but excluded from queries.
        
        **Notes:**
        - Returns 404 if response doesn't exist or is already removed
        - Updates last_update timestamp
        - Response won't appear in listings or GET requests
        
        **Authorization:**
        - Admins: Can delete any response
        - Respondents: Can only delete their own responses (matched by email)
      security:
        - bearerAuth: []
        - apiToken: []
        - respondentToken: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            maxLength: 64
          description: Response ID
      responses:
        "200":
          description: Response deleted successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  response_id:
                    type: string
                  message:
                    type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Response not found
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                  code:
                    type: string
                  message:
                    type: string
        "500":
          $ref: "#/components/responses/ServerError"
  /survey:
    get:
      tags:
        - Public
      summary: List surveys for token
      security:
        - respondentToken: []
        - respondentTokenQuery: []
      parameters:
        - name: token
          in: query
          required: false
          schema:
            type: string
            maxLength: 128
          deprecated: true
          description: (Deprecated) Respondent token in query. Prefer X-Respondent-Token header.
        - name: email
          in: query
          required: false
          schema:
            type: string
            format: email
          description: Optional email lookup (if implemented).
      responses:
        "200":
          description: Survey list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
                      properties:
                        public_id:
                          type: string
                        title:
                          type: string
                        open_on:
                          type:
                            - string
                            - "null"
                          format: date-time
                        close_on:
                          type:
                            - string
                            - "null"
                          format: date-time
                      required:
                        - public_id
                  message:
                    type: string
                required:
                  - items
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
  /survey/{public_id}:
    get:
      tags:
        - Public
      summary: Get survey snapshot
      security:
        - respondentToken: []
        - respondentTokenQuery: []
      parameters:
        - $ref: "#/components/parameters/PublicIdPath"
        - $ref: "#/components/parameters/TokenQuery"
        - name: include_response
          in: query
          required: false
          schema:
            type: boolean
            default: true
        - name: locale
          in: query
          required: false
          schema:
            type: string
        - name: response_mode
          in: query
          required: false
          schema:
            type: string
            enum:
              - latest
              - all
            default: latest
          description: Whether to include only the latest response attempt or all attempts in SurveyPublic.
        - name: response_id
          in: query
          required: false
          schema:
            type: string
            maxLength: 64
          description: If provided, return this specific attempt as `response`.
        - name: token
          in: query
          required: false
          deprecated: true
          schema:
            type: string
          description: (Deprecated) Respondent token. Prefer X-Respondent-Token header.
      responses:
        "200":
          description: Survey payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SurveyPublic"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Public
      summary: Save answers
      security:
        - respondentToken: []
        - respondentTokenQuery: []
      parameters:
        - $ref: "#/components/parameters/PublicIdPath"
        - $ref: "#/components/parameters/TokenQuery"
        - name: new_attempt
          in: query
          required: false
          schema:
            type: boolean
            default: false
          description: If true and campaign allows, creates a new attempt and saves into it.
        - name: token
          in: query
          required: false
          deprecated: true
          schema:
            type: string
          description: (Deprecated) Respondent token. Prefer X-Respondent-Token header.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResponseUpsert"
      responses:
        "200":
          description: Saved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
        - Public
      summary: Delete answers
      security:
        - respondentToken: []
        - respondentTokenQuery: []
      parameters:
        - $ref: "#/components/parameters/PublicIdPath"
        - $ref: "#/components/parameters/TokenQuery"
        - name: token
          in: query
          required: false
          deprecated: true
          schema:
            type: string
          description: (Deprecated) Respondent token. Prefer X-Respondent-Token header.
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"

  /unsubscribe:
    get:
      tags:
        - Unsubscribe
        - Public
      summary: Unsubscribe from emails
      description: Unsubscribe link handler used in email footer. The signed token encodes scope (snapshot/global) and target snapshot (if any).
      security: []
      parameters:
        - name: u
          in: query
          required: true
          schema:
            type: string
          description: Signed unsubscribe token from email footer.
        - name: reason
          in: query
          required: false
          schema:
            type: string
            maxLength: 256
      responses:
        "200":
          description: Unsubscribed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnsubscribeResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Unsubscribe
        - Public
      summary: Unsubscribe from emails (API)
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnsubscribeTokenRequest"
      responses:
        "200":
          description: Unsubscribed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnsubscribeResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/ServerError"

  /campaigns/{id}/email-log:
    get:
      tags:
        - EmailAudit
      summary: List campaign email delivery log
      description: Per-recipient send records with current delivery status (driven by provider events, e.g. SES delivered/bounce/complaint).
      parameters:
        - $ref: "#/components/parameters/IdPath16"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Offset"
        - name: email_type
          in: query
          required: false
          schema:
            type: string
            enum: [invite, reminder]
        - name: status
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Email log list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailLogList"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
  /campaigns/{id}/email-log/{email_log_id}/events:
    get:
      tags:
        - EmailAudit
      summary: List delivery events for an email record
      parameters:
        - $ref: "#/components/parameters/IdPath16"
        - name: email_log_id
          in: path
          required: true
          schema:
            type: integer
            format: int64
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Offset"
      responses:
        "200":
          description: Delivery events
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailDeliveryEventList"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
  /campaigns/{id}/respondents:
    get:
      tags:
        - Campaigns
      summary: List campaign respondents
      parameters:
        - $ref: "#/components/parameters/IdPath16"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Offset"
        - name: q
          in: query
          required: false
          schema:
            type: string
          description: Search by email substring
      responses:
        "200":
          description: Respondent list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/Respondent"
                  page:
                    $ref: "#/components/schemas/Pagination"
                required:
                  - items
                  - page
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Campaigns
      summary: Add/update respondents (bulk)
      description:
        Registers respondents for a campaign. Server generates tokens when missing (recommended) and returns created/updated
        counts.
      parameters:
        - $ref: "#/components/parameters/IdPath16"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                items:
                  type: array
                  items:
                    $ref: "#/components/schemas/RespondentUpsert"
                send_immediately:
                  type: boolean
                  default: false
                  description: If true, send invitation email after upsert.
              required:
                - items
      responses:
        "200":
          description: Bulk result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
        - Campaigns
      summary: Remove respondents (bulk)
      parameters:
        - $ref: "#/components/parameters/IdPath16"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                respondent_ids:
                  type: array
                  items:
                    type: string
                    maxLength: 32
                emails:
                  type: array
                  items:
                    type: string
                    format: email
                delete_responses:
                  type: boolean
                  default: false
      responses:
        "200":
          description: Bulk result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
  /campaigns/{id}/responses/stats:
    get:
      tags:
        - Campaigns
      summary: Get response statistics for campaign
      description: Returns counts of registered respondents, in-progress responses, and completed responses
      parameters:
        - $ref: "#/components/parameters/IdPath16"
      responses:
        "200":
          description: Response statistics
          content:
            application/json:
              schema:
                type: object
                properties:
                  campaign_id:
                    type: string
                  is_public:
                    type: boolean
                  total_respondents:
                    type: integer
                    description: Total registered respondents (0 for public campaigns)
                  in_progress:
                    type: integer
                    description: Number of partially completed responses
                  completed:
                    type: integer
                    description: Number of fully completed responses
                  total_responses:
                    type: integer
                    description: Total responses (in_progress + completed)
                required:
                  - campaign_id
                  - is_public
                  - total_respondents
                  - in_progress
                  - completed
                  - total_responses
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
  /campaigns/{id}/responses/export:
    get:
      tags:
        - Campaigns
      summary: Export campaign responses
      description: |
        Export responses with optional filtering and text enhancement.
        
        **Status filtering:** Select which response statuses to include (in_progress, completed, or both)
        
        **Text enhancement:** 
        - `includeQuestionText`: Adds human-readable question text from survey definition
        - `includeAnswerText`: For choice-based questions, adds text labels for selected choices
        
        **Format:** Currently supports JSON export. Excel export coming soon.
        
        Default language from campaign is used for text extraction.
      parameters:
        - $ref: "#/components/parameters/IdPath16"
        - name: status
          in: query
          required: false
          schema:
            type: string
            default: "in_progress,completed"
          description: Comma-separated list of statuses to include (in_progress, completed)
        - name: format
          in: query
          required: false
          schema:
            type: string
            enum: [json, excel]
            default: json
          description: Export format (excel not yet implemented)
        - name: includeQuestionText
          in: query
          required: false
          schema:
            type: boolean
            default: false
          description: Include human-readable question text from survey definition
        - name: includeAnswerText
          in: query
          required: false
          schema:
            type: boolean
            default: false
          description: Include human-readable answer text for choice-based questions
      responses:
        "200":
          description: Exported responses
          content:
            application/json:
              schema:
                type: object
                properties:
                  campaign_id:
                    type: string
                  export_date:
                    type: string
                    format: date-time
                  filters:
                    type: object
                    properties:
                      status:
                        type: array
                        items:
                          type: string
                      includeQuestionText:
                        type: boolean
                      includeAnswerText:
                        type: boolean
                  responses:
                    type: array
                    items:
                      type: object
                      properties:
                        response_id:
                          type: string
                        respondent_id:
                          type: string
                        email:
                          type: string
                        status:
                          type: string
                          enum: [in_progress, completed]
                        attempt_no:
                          type: integer
                        submitted_at:
                          type: string
                          format: date-time
                          nullable: true
                        created:
                          type: string
                          format: date-time
                        data:
                          type: object
                          description: Response data (enhanced with questionText/answerText if requested)
                  count:
                    type: integer
                required:
                  - campaign_id
                  - export_date
                  - filters
                  - responses
                  - count
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "501":
          description: Excel format not yet implemented
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/ServerError"
  /blacklist:
    get:
      tags:
        - Unsubscribe
      summary: List blacklist entries
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Offset"
        - name: email
          in: query
          required: false
          schema:
            type: string
            format: email
        - $ref: "#/components/parameters/SnapshotIdQuery"
      responses:
        "200":
          description: Blacklist list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/BlacklistEntry"
                  page:
                    $ref: "#/components/schemas/Pagination"
                required:
                  - items
                  - page
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Unsubscribe
      summary: Add blacklist entry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlacklistUpsert"
      responses:
        "200":
          description: Added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BlacklistEntry"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
        - Unsubscribe
      summary: Remove blacklist entry
      parameters:
        - name: black_list_id
          in: query
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
  /storage/items:
    get:
      tags:
        - Storage
      summary: List storage items
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Offset"
        - name: path
          in: query
          required: false
          schema:
            type: string
          description: Filter by local_path prefix
        - name: q
          in: query
          required: false
          schema:
            type: string
          description: Search by name substring
      responses:
        "200":
          description: Storage list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/StorageItem"
                  page:
                    $ref: "#/components/schemas/Pagination"
                required:
                  - items
                  - page
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Storage
      summary: Create storage item + get pre-signed upload URL
      description: Creates metadata record and returns a pre-signed upload descriptor (S3).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StorageCreateRequest"
      responses:
        "200":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PresignedUpload"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
  /storage/items/{id}:
    get:
      tags:
        - Storage
      summary: Get storage item
      parameters:
        - $ref: "#/components/parameters/IdPath16"
      responses:
        "200":
          description: Storage item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageItem"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
        - Storage
      summary: Update storage item metadata
      parameters:
        - $ref: "#/components/parameters/IdPath16"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  maxLength: 256
                description:
                  type: string
                local_path:
                  type: string
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StorageItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
        - Storage
      summary: Delete storage item
      parameters:
        - $ref: "#/components/parameters/IdPath16"
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                required:
                  - ok
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
  /survey/{public_id}/attempts:
    post:
      tags:
        - Public
      summary: Start a new response attempt
      description: Creates a new response attempt for a respondent (if campaign.allow_multiple_responses allows).
      security:
        - respondentToken: []
        - respondentTokenQuery: []
      parameters:
        - $ref: "#/components/parameters/PublicIdPath"
        - $ref: "#/components/parameters/TokenQuery"
        - name: token
          in: query
          required: false
          deprecated: true
          schema:
            type: string
          description: (Deprecated) Respondent token. Prefer X-Respondent-Token header.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                client_meta:
                  type: object
                  additionalProperties: true
              additionalProperties: false
      responses:
        "200":
          description: Created attempt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Response"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /survey/{public_id}/attachments:
    post:
      tags:
        - Public
        - Storage
      summary: Initiate file upload (respondent)
      description: Returns a pre-signed S3 upload descriptor (max 50 MB, restricted MIME categories) and creates a pending attachment linked to the response attempt.
      security:
        - respondentToken: []
        - respondentTokenQuery: []
      parameters:
        - $ref: "#/components/parameters/PublicIdPath"
        - $ref: "#/components/parameters/TokenQuery"
        - name: token
          in: query
          required: false
          deprecated: true
          schema:
            type: string
          description: (Deprecated) Respondent token. Prefer X-Respondent-Token header.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PublicUploadInitRequest"
      responses:
        "200":
          description: Upload initiated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicUploadInitResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /survey/{public_id}/attachments/{upload_id}/complete:
    post:
      tags:
        - Public
        - Storage
      summary: Complete file upload (respondent)
      security:
        - respondentToken: []
        - respondentTokenQuery: []
      parameters:
        - $ref: "#/components/parameters/PublicIdPath"
        - $ref: "#/components/parameters/TokenQuery"
        - name: upload_id
          in: path
          required: true
          schema:
            type: string
            maxLength: 64
        - name: token
          in: query
          required: false
          deprecated: true
          schema:
            type: string
          description: (Deprecated) Respondent token. Prefer X-Respondent-Token header.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PublicUploadCompleteRequest"
      responses:
        "200":
          description: Completed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicUploadCompleteResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/ServerError"
  /respondents/{respondent_id}/responses:
    get:
      tags:
        - Responses
      summary: List response attempts for a respondent
      parameters:
        - name: respondent_id
          in: path
          required: true
          schema:
            type: string
            maxLength: 32
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Offset"
      responses:
        "200":
          description: Response list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/Response"
                  page:
                    $ref: "#/components/schemas/Pagination"
                required:
                  - items
                  - page
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/ServerError"
